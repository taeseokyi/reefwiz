# 2026 여름 기준선 스냅샷 (2026-07-04 보관, 2026-08-04 아카이브로 일부 통합)

겨울철 초기 불안정("언덕") 변화를 비교하기 위한 여름 상태 보존본.

## ★2026-08-04: 평탄 이력은 아카이브로 통합됨

이 스냅샷이 생긴 이유는 평탄 이력이 42런=14일 롤링이라 겨울이 되면 여름 커브가 전부
사라진다는 것이었다. **`data/dkh_plateau_archive.jsonl`(append-only, 무기한 보관)이
생기면서 그 이유가 없어졌다** — 여기 있던 `dkh_plateau_history_20260704.json` 10런은
아카이브에 **전 필드 그대로** 들어 있고(검증 완료), 오히려 아카이브 쪽에는 나중에
백필된 `co2_suspect`·`ref_net_mph`가 더 붙어 있다. 두 벌을 두면 갈라지기만 하므로
중복 파일은 제거했다(git 이력에는 남아 있다).

여름 커브를 볼 때는 아카이브에서 기간으로 걸러 쓴다:

```python
import plateau_archive
runs = [r for r in plateau_archive.load() if r["run_started"] < "2026-07-05"]
```

아카이브는 2026-07-01 05:00부터 시작한다(그 이전 커밋 스냅샷이 없어 복원 불가).
**이보다 앞선 기간을 보존해야 하는 계절 스냅샷이 앞으로 생긴다면** 아카이브로는 못
덮으므로 그때는 이 디렉터리 방식이 여전히 필요하다.

## 파일

- `measure_kh_20260701-0704.log` — 같은 기간 원시 측정 로그(V4, 210KB).
  아카이브는 **파싱된 궤적만** 담으므로 `[RAW]` ADC 값·모터 명령·경고 원문 같은 건
  이 로그에만 있다. 그래서 이건 계속 여기 둔다(원본은 1MB 넘으면 덮어써진다).
- ~~`dkh_plateau_history_20260704.json`~~ — 아카이브로 통합, 위 참조.

기억해 둘 런(아카이브에서 같은 run_started 로 찾을 것):

- `2026-07-03 05:00:02` = false lock 사례 원본(tank_n=8에서 조기 잠금, 7.509)
- `2026-07-04 05:00:02` = FLAT_MIN_N_TANK=20 첫 실전 검증 성공(언덕 재현 → n=45 잠금, 7.255)
- `2026-07-02 21:00:03` = BT 사망 미완주 런(completed=False)

## 여름 조건 요약 (비교 기준)

- 실내: 여름(에어컨 유무 혼재), 본수조 26.6°C, 밀폐 챔버 내부 28.9~30.5°C → 시료 **+2.8°C 승온** 과도
- 진짜 평형 tank pH: 7.816~7.819 (나흘 연속 사실상 고정)
- 진폭(시작pH−평형pH): 낮 99~123 mpH / 새벽 35~38 mpH (새벽=저진폭=false lock 취약)
- 초기 언덕: 첫 1~7샘플 평탄(span ≤1 mpH)한 뒤 하강 시작하는 형태, 방향은 하강(승온+CO₂ 동방향)
- tank 잠금 소요: 정상 45~78샘플(29~50분), ref: 8~15샘플
- 도징: 올포리프 자동 2mL×6/일(유지 수준), ref_kh 앵커 8.830

## 겨울에 볼 것

챔버 온도 하강 → ΔT 부호 반전(시료가 식는 쪽) 가능 → 열(상승)·CO₂(하강) 과도가 반대 방향이 되면
n=20 이후 "중턱 어깨"(기울기 0 통과)형 false lock 위험 — MIN_N 게이트가 못 막는 유일한 모양.
겨울 데이터를 이 스냅샷과 같은 형식으로 비교할 것.

**★주의(2026-07-23 측정 방식 전환):** 이 기간은 "폭기 중 전체측정" 구 스킴이고, 7/23부터는
"고정 사전폭기(tank 25분/ref 3.5분) + 읽는 순간만 폭기 OFF"로 바뀌었다. flat_n·곡선 모양을
7/23 이후 런과 직접 비교하면 안 된다.
